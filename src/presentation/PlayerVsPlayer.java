package presentation;

import javax.swing.*;
import java.awt.*;

public class PlayerVsPlayer extends JPanel {
    public PlayerVsPlayer() {
        setLayout(new BorderLayout());
        add(new JLabel("Modo Jugador vs Jugador - En construcción", SwingConstants.CENTER));
    }
}